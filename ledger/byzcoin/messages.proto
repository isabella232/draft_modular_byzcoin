syntax = "proto3";

package byzcoin;

import "blockchain/skipchain/types.proto";

message Transaction {
    string key = 1;
    bytes value = 2;
}

message StateMessage {
    skipchain.ProofMessage proof = 1;
    bytes value = 2;
}
